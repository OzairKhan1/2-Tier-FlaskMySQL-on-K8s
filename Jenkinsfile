@Library("jShrLibs") _
pipeline {
    agent {label "docNode"}

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World! Just to Check Webhooks'
            }
        }
    }
}
