@Library("jShrLibs") _
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World! Just for Checking Webhook'
            }
        }
    }
}
